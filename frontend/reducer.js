import { ADD_FOOD, CLEAR } from "./actions";

const _defaultState = {
  foods: []
}

const reducer = (oldState = _defaultState, action) => {
  switch(action.type) {
    
    default:
      return oldState;
  }
}

export default reducer;
